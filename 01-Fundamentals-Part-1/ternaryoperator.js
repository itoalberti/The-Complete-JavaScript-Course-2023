const age = 17;

const canDrink = age >= 18 ? 'You can have some beer, paw' : 'Better stick to your soymilk, boy'
console.log(canDrink)

// Syntax:
// const A = [conditionForValidation] ?[if] [consequence if true] :[else] [consequence if false]